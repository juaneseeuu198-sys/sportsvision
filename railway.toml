[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && gunicorn sportsvision.wsgi --log-file -"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
