[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py seed_exercises && python manage.py setup_admin && gunicorn sportsvision.wsgi --log-file -"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
